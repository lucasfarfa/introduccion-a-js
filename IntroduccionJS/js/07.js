// Orden de las opepraciones en js.

let resultado;

resultado = 20 + 30 * 2; // se realiza primero 30*2 y luego la suma
resultado = (20 + 30) * 2; // igual que en el colegio

resultado = (100 + 200 + 300) * 1.21; // suma total + el impuesto IVA 21% 


//console.log(resultado);

// Incrementos

let puntaje = 10;
puntaje ++; //muestra puntaje luego incrementa
++puntaje; //incnrementa luego muestra

// Decrementos
puntaje--; //funciona igual que el ++
--puntaje; 

// Incrementos / dec mayores a uno
puntaje += 10; // incrementa 10 de una;

console.log(puntaje);