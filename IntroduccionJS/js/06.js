// Objeto Math. Metodos mas utlizados.

let resultado;

resultado = Math.PI; //asigna a la variable el valor de pi.
resultado = Math.round(2.2); // redondea el numero. 2.5 = 3, 2.4 para abajo = 2.
resultado = Math.ceil(2.2); // Ceil siempre redondea hacia arriba
resultado = Math.floor(2.2); // siempre redondea hacia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-200); // funcion modulo
resultado = Math.min(3,5,7,8,1); // minimo de un listado
resultado = Math.max(3,5,7,8,1); // maximo de un listado
resultado = Math.random(); //genera numero aleateorio por lo general menor a 1.
resultado = Math.floor(Math.random() * 30); // num aleatoreo mayor a 1

console.log(resultado);