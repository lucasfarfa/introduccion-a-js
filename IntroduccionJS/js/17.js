// Diferencia entre Metodo y funcion.
const num1 = 20;
const num2 = '20';

console.log(parseInt(num2)); // parseint() es una funcion por su estructura, transforma str a int
console.log(num1.toString()); // metodo porque primero va VAR.METODO, lo diferencia de func el punto